Prerequisites
-------------
Python 3.7! TF 1.x is broken on 3.8 and above and 3.5 and below.

python -m pip install -r requirements.txt

Using quote feature
-------------------
Place the `checkpoints` directory into the current folder.
The code will pick up the latest checkpoint from there.
